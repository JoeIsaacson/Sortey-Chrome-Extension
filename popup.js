//Joey Isaacson copyrighted yadadada//

chrome.history.search(object query, function callback);

chrome.windows.update(200 '#main', object updateInfo, function callback);



// Run the Sortey scrape once everthing is ready
document.addEventListener('DOMContentLoaded', function () {
  scrape.history();
});
